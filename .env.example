# Long-lived access token from your Home Assistant profile page
# (Profile -> Security -> Long-lived access tokens).
HA_TOKEN=

# Optional: require X-API-Key on the local HTTP API.
HPMPC_API_KEY=

TZ=Europe/Stockholm
